name: Setup Codespace for Convex Tutorial

on:
  codespace:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome
        run: |
          echo "🚀 Setting up your Convex tutorial environment!"
          echo "This project includes:"
          echo "  - React frontend with Vite"
          echo "  - Convex backend"
          echo "  - TypeScript configuration"
